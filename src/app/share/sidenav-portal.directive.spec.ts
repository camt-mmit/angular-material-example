import { SidenavPortalDirective } from './sidenav-portal.directive';

describe('SidenavPortalDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavPortalDirective();
    expect(directive).toBeTruthy();
  });
});
